#include "main.h"

/**
 * _isupper - If is uppercase return 1, if not return 0
 *
 * @c: this is the entry
 *
 * Return: 0
 *
 **/
int _isupper(int x)

{

if (x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
